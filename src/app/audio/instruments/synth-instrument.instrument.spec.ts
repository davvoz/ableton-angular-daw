import { SynthInstrument } from './synth-instrumentinstrument';

describe('SynthInstrument', () => {
  it('should create an instance', () => {
    expect(new SynthInstrument()).toBeTruthy();
  });
});
